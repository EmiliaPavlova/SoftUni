var app = app || {};

app.data = (function () {

    var credentials = (function() {
        function getHeaders(contentType, useSession) {
            if(contentType) {
                this.contentType = contentType;
            }

            this
        }
    });

    var users = (function () {

    });

    var posts = (function () {

    });

    return {

    }
});